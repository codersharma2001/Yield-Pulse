[build]
  command = "pnpm turbo run build --filter=@yield-dashboard/web"
  publish = "apps/web/.next"

[build.environment]
  NETLIFY_USE_PNPM = "true"
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
