# Docker & Deployment Assets

Dockerfiles, docker-compose stacks, and optional Kubernetes manifests will be staged here. Future iterations will add a one-click local demo that provisions contracts, the API, frontend, Redis, and Tenderly simulation proxies.
